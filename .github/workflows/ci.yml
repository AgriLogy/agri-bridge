name: CI

# Thin caller into the org-shared Node test gate (npm ci + npm test).

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    uses: AgriLogy/shared-workflows/.github/workflows/node-test.yml@v1
    with:
      node_version: "20"
